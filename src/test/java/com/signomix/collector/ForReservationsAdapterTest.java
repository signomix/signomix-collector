package com.signomix.collector;

import io.quarkus.test.junit.QuarkusTest;

@QuarkusTest
class ForReservationsAdapterTest {
/*     @Test
    void testHelloEndpoint() {
        given()
                .when().get("/api/reservations/hello")
                .then()
                .statusCode(200)
                .body(is("Hello from reservations REST API"));
    }

    @Test
    void testStatusEndpoint() {
        given()
                .when().get("/api/reservations/status/1")
                .then()
                .statusCode(200)
                .body(is("1"));
    }

    @Test
    void testSetStatusEndpoint() {
        ReservationDto reservation = new ReservationDto();
        reservation.roomId = 1;
        reservation.status = 1;
        given().contentType("application/json")
                .body(reservation)
                .when().post("/api/reservations/status")
                .then()
                .statusCode(200)
                .body(is("OK"));
    } */

}